## 1. 数据库与数据初始化

- [x] 1.1 在 SQLite 中创建用户公告已读关联表 `sys_notice_read`
- [x] 1.2 在 `sys_menu` 表中注入“登录日志”菜单（层级与用户管理并列）
- [x] 1.3 为 `root` 角色分配对应的登录日志菜单和按钮权限 (写入 `sys_role_menu`)

## 2. 看板与登录审计开发 (子代理 A 负责)

- [x] 2.1 创建登录日志 `LoginLog` 实体类、Mapper、Service 核心接口与实现
- [x] 2.2 在 AuthController 拦截或 AuthServiceImpl 登录事件中调用登录日志保存接口，实现写记录
- [x] 2.3 编写登录日志的后台 ECharts 数据统计 `/statistics` 接口
- [x] 2.4 前端 package.json 引入并在 npm 中安装 `echarts` 依赖
- [x] 2.5 重构 `HomeView.vue` 界面，集成 ECharts 并渲染最近 7 天活跃与浏览器饼图
- [x] 2.6 编写 `views/system/LoginLogManagement.vue` 日志审计界面，配置对应的路由

## 3. 消息通知与已读未读服务开发 (子代理 B 负责)

- [x] 3.1 创建已读关系表对应的 `NoticeRead` 实体类、Mapper 和 Service 核心类
- [x] 3.3 编写未读公告数查询 `/unread-count`、单条已读 `/read/{id}`、一键全部已读 `/read-all` 接口
- [x] 3.4 重构现有的公告查询列表服务，将已读/未读状态通过连表计算包含在公告实体出参中返回
- [x] 3.5 前端顶栏（`Header.vue`）右上角添加铃铛图标及未读数 Badge，点击滑出未读消息抽屉（Drawer）
- [x] 3.6 前端实现登录首屏强提醒：用户登录进入首页时，若有最新未读公告，进行毛玻璃弹框卡片提醒

## 4. 全栈联合调试与验证

- [x] 4.1 清空后端菜单缓存，重启全栈服务
- [x] 4.2 使用 `admin` 用户登录系统，操作公告新增、已读，验证铃铛数和数据大盘刷新
- [x] 4.3 新增一个普通角色并分配登录日志查看与公告管理查看，验证该用户登录后能查公告（无增删改）、能查首页但侧边栏无登录日志菜单
